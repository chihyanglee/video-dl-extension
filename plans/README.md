# Implementation Plans

Generated by the improve skill on 2026-06-19, against commit `16db614`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a runnable test suite (vitest) for the pure parsers/crypto | P1 | M | — | DONE (22 tests; branch `advisor/001-test-suite`) |
| 002 | Serialize detection writes to fix the concurrent read-modify-write race | P1 | M | 001 | DONE (branch `advisor/002-serialize-detection-writes`) |
| 003 | Fix the offscreen-document START race (ready handshake) | P1 | S–M | — | DONE (branch `advisor/003-offscreen-start-handshake`) |
| 004 | Add CI (GitHub Actions): typecheck + build + test on push/PR | P2 | S | 001 | DONE (branch `advisor/004-ci`) — see merge note |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Notes from execution

- **001 DONE** on branch `advisor/001-test-suite` (worktree, not merged).
  `vitest` was pinned to **`^2.1.9`**, not latest (v4): vitest 4 requires Vite 6
  but this repo pins Vite `^5.4`, and v4 crashes at startup
  (`ERR_PACKAGE_PATH_NOT_EXPORTED vite/module-runner`). Executors of 002/004
  should keep the `^2` pin — vitest is already installed; do not re-add it.
  Tests live at `src/shared/m3u8.test.ts`, `src/shared/mpd.test.ts`,
  `src/offscreen/decrypt.test.ts`. `pnpm test` / `pnpm test run` work.

## Merge order (all four executed on isolated worktrees off `16db614`)

Each plan was executed in its own worktree branched from `16db614`. They touch
disjoint source files and are merge-clean; merge in this order:

1. `advisor/001-test-suite` — adds vitest (`^2.1.9`), `test` script, parser/crypto tests.
2. `advisor/002-serialize-detection-writes` — fixes the X detection race. Also
   adds vitest + `test` script (identical to 001 → resolves to the same lines).
3. `advisor/003-offscreen-start-handshake` — fixes the dropped-download race.
4. `advisor/004-ci` — adds `.github/workflows/ci.yml`. Also adds vitest + `test`
   script (identical to 001). **Its CI "Test" step only goes green once 001's
   `*.test.ts` files are present on the branch CI checks out** — i.e. after these
   are merged together. Verified standalone: yaml is correct, typecheck + build
   pass; `pnpm test run` fails in the bare 004 worktree only because the test
   files live on 001's branch (worktree isolation), not a defect in 004.

002 and 004 each independently added the vitest devDep + `test` script because
their worktrees branched off `16db614` (which predates 001). After merging 001
first, those become no-op duplicate lines.

## Dependency notes

- **002 depends on 001**: the detection-race fix needs the test harness from 001
  (vitest + an in-memory `chrome.storage` mock) to verify that concurrent
  renditions all land. Do 001 first.
- **004 depends on 001**: the CI `test` step requires the `pnpm test` script that
  001 introduces. If 004 runs before 001, omit the test step and add it later.
- 003 is independent of the others.

## Findings considered and rejected (do not re-audit)

- `fast-xml-parser <5.7.0` moderate advisory (GHSA-gh4j-gqv2-49f6): affects
  `XMLBuilder` only; this repo uses `XMLParser` (parse) exclusively in
  `src/shared/mpd.ts` → unreachable. Major-version bump not worth the blast radius.
- `hls.js` weight in the ~673 KB side-panel chunk: assets load from the local
  extension package, not the network, so chunk size has no user-facing cost.

## Other audited findings NOT yet planned (the user deferred them)

These were surfaced and consciously deferred — plan them later if desired:

- **C3**: concurrent downloads corrupt each other (single ffmpeg singleton +
  fixed FS filenames in `src/offscreen/ffmpeg.ts`).
- **S1**: session `Cookie` captured and persisted in `storage.session` but never
  sent (`src/background/detect.ts:39`, `:55-64`).
- **TD2**: thumbnail cache in `storage.local` never evicted (`src/sidepanel/thumbnail.ts:11-15`).
- **TD3**: offscreen segment/key fetches can't set `Referer` (no DNR fallback like
  the side panel has) → hotlink-protected segment CDNs 403.
- Direction options: generalize demuxed-HLS beyond X; DASH preview via dash.js;
  subtitle extraction; stream-to-disk (File System Access).
